import React from 'react';
import { Button } from './common';


const ConfirmationModal = ({ bodyText, itemId, handleCloseModal, handleSubmitAction }) => {

  return (
    null
  )
};

export default ConfirmationModal;
